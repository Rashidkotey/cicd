This contains the ci.yml file. In this file we have the workflow named 'automating image push to dockerhub'. This versions works but it has some things that we have to change or improve. which will be done in a different file. 
NB: 
    app.py
    Dockerfile
this file are all related to this. 
app.py contains the python code that the image will will be created from and pushed to DockerHub.